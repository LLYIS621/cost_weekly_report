# generate_yyzx_data 使用说明

`generate_yyzx_data.py` 用来串联原来的三个脚本能力：

1. 生成并更新月度 `optCost`
2. 生成并更新近四周 `optCost`
3. 读取数据库 3 张表并导出本地 BI 文件

## 文件位置

- 主脚本：`D:\yyzx_data\generate_yyzx_data.py`
- BI 输出：`D:\yyzx_data\运营团队BI报表.xlsx`
- 运行日志目录：`D:\yyzx_data\logs`

## 常用运行方式

全量执行：

```powershell
python D:\yyzx_data\generate_yyzx_data.py
```

只跑月度写库：

```powershell
python D:\yyzx_data\generate_yyzx_data.py --only monthly
```

只跑周度写库：

```powershell
python D:\yyzx_data\generate_yyzx_data.py --only weekly
```

只导出 BI 文件：

```powershell
python D:\yyzx_data\generate_yyzx_data.py --only bi
```

指定月度写库起始日期：

```powershell
python D:\yyzx_data\generate_yyzx_data.py --start-date 2026-05-01
```

## 日志说明

每次运行会生成两类文件：

1. 运行日志：`generate_yyzx_data_时间戳.log`
2. 核对日志：`check_report_时间戳.xlsx`

核对日志里目前重点看：

- `运行概览`
  - 每个任务的目标表或输出文件
  - 源数据行数
  - 输出行数
  - 总消耗
  - 日期范围 / 周标签 / BI 源表规模
- `monthly_opt_cost`
  - 月度写库按年汇总的核对结果
- `weekly_opt_cost`
  - 周度写库按周标签汇总的核对结果
- `bi_export`
  - BI 导出汇总

## BI 文件口径说明

`运营团队BI报表.xlsx` 中的 `团队维度` sheet 由 `generate_yyzx_data.py` 的 `build_team_dimension_table()` 生成。下面这部分记录的是当前脚本实际使用的统计逻辑。

### 维度层级

团队类数据会按以下粒度出数：

- `所属中心`
- `部门`
- `组别`
- `姓名`
- 特殊汇总行 `K部门`
- 特殊汇总行 `整体`

其中：

- `K部门` 不是源表原字段，而是把 `K2`、`K3`、`K4`、`K5`、`K7` 在 `运营中心` 下汇总后生成的部门级虚拟行。
- `整体` 也不是源表原字段，而是把所有中心合并后的总计行。输出时会写成 `所属中心=整体`、`运营部门=整体`。

### 时间粒度

`团队维度` 会生成四种时间粒度：

- `周`
- `月`
- `季度`
- `年`

其中：

- `周` 的 `开始日期` 为周一，`结束日期` 为周日。
- `月`、`季度`、`年` 的 `开始日期` 和 `结束日期` 使用自然周期边界，但如果周期尚未结束，`天数` 会按当前报表最新消耗日期截断。

### 基础指标

#### 消耗

- 公式：同一统计口径下 `消耗` 求和。

#### 天数

- `周`: 固定为 7 天。
- `月`: 取该月自然天数；若是当前未完结月份，则截止到最新消耗日期。
- `季度`: 取该季度自然天数；若是当前未完结季度，则截止到最新消耗日期。
- `年`: 从 `1 月 1 日` 到最新消耗日期的天数。

#### 日均消耗

- 公式：`日均消耗 = 消耗 / 天数`

### 人力指标来源

人力相关字段来自数据库表 `月度人力数据源`，只取 `用途='运营团队用'` 的数据。

- `人效人数` 使用字段 `运营人效人数`
- `项目人数` 使用字段 `运营项目人数`

脚本会先按月汇总人力，再派生季度和年度口径：

- `月`: 直接取当月值
- `季度`: 取季度内各月的平均值
- `年`: 取年内各月的平均值

### 周维度的人效人数 / 项目人数

周维度不单独按周做人力快照，而是映射到月度人力数据：

1. 先用该周 `开始日期` 所属月份取值
2. 如果该月份没有对应人力数据，再用该周 `结束日期` 所属月份取值
3. 如果两边都没有，则保留 `NaN`

例如跨年周 `2025-12-29 ~ 2026-01-04`：

- 先尝试 `2025M12`
- 取不到再尝试 `2026M01`
- 都取不到则返回空值

### 品牌数 / 产品数 / 账户数

#### 唯一键

脚本先构造三类唯一键：

- `品牌数`: `媒体|品牌名称`
- `产品数`: `媒体|产品名称`
- `账户数`: `媒体|账户ID`

如果对应原字段为空，则该条记录不参与该指标统计。

#### 周粒度

周粒度使用自然去重：

- `品牌数`: 统计分组内 `品牌唯一键` 去重个数
- `产品数`: 统计分组内 `产品唯一键` 去重个数
- `账户数`: 统计分组内 `账户唯一键` 去重个数

#### 月粒度

月粒度不是简单去重，而是按活跃天数折算：

1. 先按 `统计分组 + 唯一键` 统计该月内有消耗的去重天数
2. 再把每个唯一键按活跃天数换算成权重

权重规则：

- 活跃天数 `< 7`: 记 `0`
- 活跃天数 `7 ~ 13`: 记 `0.5`
- 活跃天数 `>= 14`: 记 `1`

最后把权重求和，得到月度的 `品牌数`、`产品数`、`账户数`。

#### 季度和年粒度

季度和年粒度的 `品牌数`、`产品数`、`账户数` 不直接基于整季或整年明细去重，而是：

- 先按月算出每个月的上述指标
- 再对季度内各月取平均，得到季度值
- 再对年度内各月取平均，得到年度值

这意味着季度和年口径反映的是“月均有效品牌数 / 项目数 / 账户数”，不是全周期并集去重。

### 派生指标公式

#### 人均日耗

- 公式：`人均日耗 = 日均消耗 / 人效人数`
- 当 `人效人数` 为 `0` 或空值时，结果记为 `NaN`

#### 人均品牌

- 公式：`人均品牌 = 品牌数 / 项目人数`
- 当 `项目人数` 为 `0` 或空值时，结果记为 `NaN`

#### 人均账户

- 公式：`人均账户 = 账户数 / 项目人数`
- 当 `项目人数` 为 `0` 或空值时，结果记为 `NaN`

#### 人均项目

- 公式：`人均项目 = 产品数 / 项目人数`
- 当 `项目人数` 为 `0` 或空值时，结果记为 `NaN`

#### 户均消耗

- 公式：`户均消耗 = 日均消耗 / 账户数`
- 等价于：`消耗 / 天数 / 账户数`
- 当 `账户数` 为 `0` 或空值时，结果记为 `NaN`

### 多级团队聚合逻辑

#### 所属中心

- 以 `所属中心` 为聚合键汇总消耗与去重指标
- `运营部门`、`组别`、`姓名` 置空

#### 部门

- 以 `所属中心 + 运营部门` 为聚合键
- `组别`、`姓名` 置空

#### 组别

- 以 `所属中心 + 运营部门 + 组别` 为聚合键
- `姓名` 置空

#### 姓名

- 以 `所属中心 + 运营部门 + 组别 + 姓名` 为聚合键

### K部门聚合逻辑

`K部门` 是额外生成的一层部门级汇总，不影响原始部门行。

统计方式：

1. 先从明细中过滤 `所属中心=运营中心` 且 `运营部门 in [K2, K3, K4, K5, K7]`
2. 再按部门级口径汇总消耗、品牌数、产品数、账户数
3. 输出时把 `运营部门` 统一写成 `K部门`

人力类指标：

- `K部门` 的 `人效人数` 和 `项目人数` 不是单独的人力表字段
- 而是把 `K2`、`K3`、`K4`、`K5`、`K7` 对应的人力值相加后作为 `K部门`

缺失处理：

- 如果 `K部门` 在某周期没有匹配到人力或项目人数，脚本会补 `0`
- 因此 `K部门` 的 `人均日耗 / 人均品牌 / 人均账户 / 人均项目` 在分母为 `0` 时会返回 `NaN`

### 整体聚合逻辑

`整体` 是额外生成的一层全量汇总，不影响各中心原始行。

统计方式：

1. 直接用全部明细汇总消耗
2. 品牌数、产品数、账户数也按“全量合并后的口径”重新计算，不是简单把各中心结果相加
3. 输出时写成 `所属中心=整体`、`运营部门=整体`

人力类指标：

- `整体` 的 `人效人数` / `项目人数` 是把各 `所属中心` 口径的人力值求和

缺失处理：

- `整体` 的 `人效人数` 取不到时会补 `0`
- `整体` 的 `项目人数` 若取不到则保留 `NaN`

### 关于求和与去重的区别

这张表里不是所有指标都能跨层级直接加总：

- `消耗`、`人效人数`、`项目人数` 可以按当前脚本定义做求和汇总
- `品牌数`、`产品数`、`账户数` 因为包含去重或活跃天数折算逻辑，不能简单把下级结果相加后视为上级真值

尤其是：

- `整体` 的品牌/产品/账户指标来自全量重新统计
- `季度`、`年` 的品牌/产品/账户指标来自月度结果平均，而不是周期内总去重

如果后续改 `generate_yyzx_data.py` 的口径，这一节也需要同步更新。

## 依赖说明

脚本会依赖旧项目里的数据库读写辅助模块：

`C:\Users\Lly621\Desktop\py_calc\code`

如果后续这个目录移动了，可以运行时显式传入：

```powershell
python D:\yyzx_data\generate_yyzx_data.py --code-dir 新目录
```

## 当前建议

日常使用建议先分段跑，再全量跑：

1. `--only monthly`
2. `--only weekly`
3. `--only bi`
4. 最后再跑一次全量

这样定位问题会更快。
